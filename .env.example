# Optional: URL of design_it_backend (no trailing slash). Defaults to https://design-it.onrender.com.
# Use http://localhost:8000 to develop against a backend running locally.
VITE_API_BASE_URL=https://design-it.onrender.com
